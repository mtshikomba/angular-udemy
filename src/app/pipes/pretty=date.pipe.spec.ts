import { Pretty=datePipe } from './pretty=date.pipe';

describe('Pretty=datePipe', () => {
  it('create an instance', () => {
    const pipe = new Pretty=datePipe();
    expect(pipe).toBeTruthy();
  });
});
